{
    "name": "crud-mock-api",
    "version": "1.0.0",
    "description": "CRUD Mock API",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "json-server --watch ./src/db.json --port 3000"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "json-server": "^1.0.0-beta.3"
    }
}
